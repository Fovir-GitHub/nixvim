_: {
  empty = {
    plugins.abolish = {
      enable = true;
    };
  };

  example = {
    plugins.abolish = {
      enable = true;
    };
  };

  defaults = {
    plugins.abolish = {
      enable = true;
    };
  };
}
